[Global]
logfile: autotest.log

kvm_server: [ENV:KVM_BUILD_SERVER]
kvm_user: [ENV:KVM_USER]
kvm_extra_label: [ENV:JOB_BASE_NAME]-[ENV:UCS_VERSION]

kvm_operating_system: UCS
kvm_template: generic-unsafe
kvm_ucsversion: [ENV:UCS_VERSION]
kvm_architecture: amd64

kvm_dhcp: 1
kvm_interface: eth0
kvm_memory: 4G
kvm_vcpus: 2

parallel: true

environment:
 UCS_VERSION=[ENV:UCS_VERSION]
 RELEASE_UPDATE=[ENV:RELEASE_UPDATE]
 ERRATA_UPDATE=[ENV:ERRATA_UPDATE]
 TARGET_VERSION=[ENV:TARGET_VERSION]

[pdn]
kvm_operating_system: Others
kvm_template: ucs-joined-master
command1:
 PUT /setup/ssh_ansible_key.pub /root/.ssh/authorized_keys
 chmod 600 /root/.ssh/authorized_keys
 LOCAL echo "[pdn]" >> "inventory.ini"
 LOCAL echo "pdn ansible_host=[ENV:pdn_IP]" >> inventory.ini
 . utils.sh && basic_setup
 . utils.sh && basic_setup_ucs_joined "[ENV:pdn_IP]"
 univention-license-import /root/license.ldif
 . utils.sh && add_tech_key_authorized_keys
 . utils.sh && jenkins_updates
 . utils.sh && assert_join
 . utils.sh && do_reboot
 . utils.sh && wait_for_reboot
files:
 _test-environment/license.ldif /root/
 _test-environment/utils/utils.sh /root/
 _test-environment/utils/utils-local.sh /root/
